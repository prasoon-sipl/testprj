dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: /.*?/
    
    commands:
      - ./deploy.sh
      - aws s3 sync /home/ubuntu/testprj s3://demoprj --delete
