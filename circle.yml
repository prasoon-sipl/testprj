dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: mybranch
    commands:
      - git branch | grep "[^* ]+" -Eo
      - aws s3 sync /home/ubuntu/testprj s3://demoprj --delete
